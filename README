weather.txt contains weather data.

day number is first column
max temp is second column
min temp is third column
Write a ruby script, which prints the day number with the smallest temperature spread.

executable via cmd
copy input file to your solutions folder or reference it from task folder